- `eval-detection --compare-results` now names exactly which metric, per-pattern
  value, or `case_id` drifted on a benchmark mismatch and prints the
  regeneration command, instead of only reporting that the files differ (#345).
