- `python scripts/check.py` runs the full required validation sequence — lint,
  format check, type check, tests, detection-benchmark guard — as one command,
  and `tests/test_check_script.py` fails if it and `.github/workflows/ci.yml`
  stop agreeing, so the local gate can no longer drift behind CI the way three
  hand-maintained copies of the checklist could (#290).
