- Redaction markers now derive from a single `redaction_marker` helper in
  `sanitization.py`, so the marker contract is defined once and tests reference
  it instead of hardcoding the literal (#344).
