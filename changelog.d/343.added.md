- Changelog fragments under `changelog.d/` so concurrent pull requests no longer
  collide on `CHANGELOG.md`; `scripts/build_changelog.py` assembles them into a
  dated section at release time (#343).
